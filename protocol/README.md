# Protocol editions

The protocol is published in two editions:

- `normative/`: authoritative protocol corpus (normative).
- `plain-language/`: plain-language mirror for readability (non-normative).

If any discrepancy exists, `normative/` governs.

